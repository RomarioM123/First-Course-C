#include <math.h>
int main()
{
	int x = 2;            //объ€вл€ем x
	int in = 1, ik = 5;   //объ€вл€ем первый и последний элемент р€да
	int sum = 0;

	for (double s = in / sin(x) * in, in == ik, in++)
	{
		sum += in;
	}
}